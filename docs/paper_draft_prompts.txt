Please read through the technical report in `docs/report/report.tex` carefully and deeply. You can view the documents, which will be displayed in the compiled PDF, but you are forbidden to view any other files that the compiled PDF won't display. After understanding this project fully, you are going to write a paper with a main file `main.tex`, a supplementary file `supp.tex`, and a BibLaTeX file `reference.bib`, and save them into the folder `docs/paper/`. Here are some notes that you must follow:
1. The title of the paper should be "AI-Assisted Conversion of R Statistical Packages to Python: A Methodological Framework".
2. The paper needs to contain all the necessary parts of an academic paper on Nature. The writing style needs to conform to the relevant standard.
3. The order of sections in the main file is approximately `Introduction`, `Results`, `Discussion`, `References`, `Methods`, and `Declarations`. You may not be informative for some elements in some parts. It is OK to leave some blanks when writing, but you cannot leave so many blanks.
4. The `Results` section should only contain the motives of the designs in each phase and the general executive frameworks as detailed as you can, where all the AI skills (commands), agents, and their calling strategies must be demonstrated (the full text of skills and agents can be placed in the supplementary, but you need to investigate them fully and cite them in the main text). You cannot leave out any considerations that I wrote about in the technical report. Also, you must think about what questions the reviewers will ask and write the paper defensively. The complex, scattered, and trivial technical details specific to the `KernSmooth` package should be stated in `Methods`. Much more complex, scattered, and trivial technical descriptions should be placed in the supplementary. Note that these three parts must refer to and cite each other!
5. In the `Discussion` part, you need to mention the limitations of the current methodology framework. For example, the `KernSmooth` package does not call other complex machine learning packages from R, unlike `glmnet`. Also, the `KernSmooth` uses the `.Fortran` interface to call the compiled Fortran functions. Other packages like `rpart` call C functions using the `.Call` interface (instead of `.C`), which passes the elements through SEXP objects in R and returns R-specific objects, making the conversion much more difficult. Discuss those things in detail, and describe what we can do next. Note that you cannot mention them anywhere else!
6. In the `Introduction` part, you need to search for recent academic papers and make the logic smooth enough towards the project. Note that `KernSmooth` is only an example demonstrated in this paper.
7. You are not allowed to add extra line breaks in the output LaTeX document. The documents should only contain line breaks that are necessary, since I have to modify it by myself.
8. You cannot draw any figure by yourself, but you definitely need to plan for many figures to demonstrate the workflow of our methodology. When it comes to a figure, you need to insert a placeholder and tell me in the response text about all the details of the figure.