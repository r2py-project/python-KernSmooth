{
  "imports": [
    "from typing import Any",
    "import numpy as np"
  ],
  "function_prototype": "def blkest(x: np.ndarray[Any, np.dtype[np.float64]], y: np.ndarray[Any, np.dtype[np.float64]], Nval: int, q: int) -> dict[str, float]:",
  "function_body": [
    "    # NOTE: The original R function delegates all of its numerical work to",
    "    # the compiled Fortran routine F_blkest (blkest.f), whose source is not",
    "    # available in this context. This is a best-effort reconstruction of",
    "    # that routine based on its documented purpose (Ruppert, Sheather &",
    "    # Wand, 1995, JASA): it partitions the (sorted) data into `Nval`",
    "    # blocks, fits a qth degree polynomial by least squares within each",
    "    # block, and pools the resulting residuals/derivatives into estimates",
    "    # of sigma^2, theta_22 = int m''(x)^2 dx and theta_24 = int m''(x) m''''(x) dx.",
    "    x = np.asarray(x, dtype=np.float64)",
    "    y = np.asarray(y, dtype=np.float64)",
    "    n = x.shape[0]",
    "",
    "    # Sort the (x, y) data with respect to the x's (as R's",
    "    # datmat[sort.list(datmat[, 1]), ] does).",
    "    order = np.argsort(x, kind=\"stable\")",
    "    x = x[order]",
    "    y = y[order]",
    "",
    "    qq = q + 1",
    "",
    "    # Partition the n sorted points into Nval blocks of roughly equal",
    "    # size; the final block absorbs any remainder.",
    "    block_size = n // Nval",
    "",
    "    sigsqe_num = 0.0",
    "    dof = 0",
    "    th22e = 0.0",
    "    th24e = 0.0",
    "",
    "    start = 0",
    "    for j in range(Nval):",
    "        end = start + block_size if j < Nval - 1 else n",
    "        xb = x[start:end]",
    "        yb = y[start:end]",
    "        nb = xb.shape[0]",
    "",
    "        if nb > q:",
    "            # Least squares fit of a qth degree polynomial to the block,",
    "            # analogous to the QR-based fit (dqrls/dqrsl-style) performed",
    "            # by the Fortran routine.",
    "            coeffs = np.polyfit(xb, yb, q)",
    "            poly = np.poly1d(coeffs)",
    "            resid = yb - poly(xb)",
    "",
    "            sigsqe_num += float(np.sum(resid ** 2))",
    "            dof += nb - qq",
    "",
    "            # Second and fourth derivatives of the fitted polynomial",
    "            # (poly.deriv beyond the polynomial's degree yields the zero",
    "            # polynomial, matching a vanishing higher-order derivative).",
    "            d2 = poly.deriv(2)",
    "            d4 = poly.deriv(4)",
    "            m2 = d2(xb)",
    "            m4 = d4(xb)",
    "",
    "            # Approximate the block's contribution to the theta_22 and",
    "            # theta_24 integrals by the average squared/pointwise product",
    "            # of the derivatives over the block, weighted by the block's",
    "            # x-range (a simple Riemann-sum-style quadrature).",
    "            span = float(xb[-1] - xb[0]) if nb > 1 else 0.0",
    "            th22e += float(np.mean(m2 ** 2)) * span",
    "            th24e += float(np.mean(m2 * m4)) * span",
    "",
    "        start = end",
    "",
    "    # Pooled residual variance estimate over all blocks.",
    "    sigsqe = sigsqe_num / dof if dof > 0 else float(\"nan\")",
    "",
    "    return {\"sigsqe\": sigsqe, \"th22e\": th22e, \"th24e\": th24e}"
  ]
}
