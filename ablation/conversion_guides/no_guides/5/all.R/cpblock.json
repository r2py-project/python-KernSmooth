{
  "imports": [
    "from typing import Any",
    "import numpy as np"
  ],
  "function_prototype": "def cpblock(X: np.ndarray[Any, np.dtype[np.float64]], Y: np.ndarray[Any, np.dtype[np.float64]], Nmax: int, q: int) -> int:",
  "function_body": [
    "    # Chooses the number of blocks for the preliminary",
    "    # step of a plug-in rule using Mallows' C_p.",
    "    #",
    "    # Note on the return value: R's `order(Cpvec)[1L]` returns the",
    "    # 1-based position of the minimum C_p value within Cpvec, where",
    "    # Cpvec[Nval] corresponds to the fit using Nval blocks (Nval running",
    "    # from 1 to Nmax). That 1-based position therefore already equals",
    "    # the optimal number of blocks Nval, which is what callers such as",
    "    # dpill use directly as a block count (e.g. blkest(x, y, Nval, 4)).",
    "    # This function returns that same integer block count N (1..Nmax),",
    "    # not a 0-based array index, to preserve the original semantics.",
    "    n = len(X)",
    "",
    "    # Sort the (X, Y) data with respect to the X's.",
    "    order_idx = np.argsort(np.asarray(X, dtype=np.float64), kind=\"mergesort\")",
    "    X = np.asarray(X, dtype=np.float64)[order_idx]",
    "    Y = np.asarray(Y, dtype=np.float64)[order_idx]",
    "",
    "    # Reimplementation of the Fortran subroutine cp (src/cp.f).",
    "    # 'q' itself is unused beyond computing qq, matching the R code's",
    "    # comment \"remove unused 'q' 2007-07-10\".",
    "    qq = q + 1",
    "    RSS = np.zeros(Nmax, dtype=np.float64)",
    "",
    "    for Nval in range(1, Nmax + 1):",
    "        # For each number of partitions",
    "        idiv = n // Nval",
    "        for j in range(1, Nval + 1):",
    "            # For each member of the partition",
    "            ilow = (j - 1) * idiv + 1",
    "            iupp = j * idiv",
    "            if j == Nval:",
    "                iupp = n",
    "            nj = iupp - ilow + 1",
    "",
    "            Xj = X[ilow - 1:iupp]",
    "            Yj = Y[ilow - 1:iupp]",
    "",
    "            # Obtain a q'th degree fit over current member of partition.",
    "            # Set up \"X\" matrix: column k (0-based) holds Xj**k.",
    "            Xmat = np.zeros((nj, qq), dtype=np.float64)",
    "            Xmat[:, 0] = 1.0",
    "            for k in range(2, qq + 1):",
    "                Xmat[:, k - 1] = Xj ** (k - 1)",
    "",
    "            # Least-squares solve via QR decomposition, mirroring the",
    "            # Fortran calls to dqrdc/dqrsl.",
    "            Qmat, Rmat = np.linalg.qr(Xmat)",
    "            coef = np.linalg.solve(Rmat, Qmat.T @ Yj)",
    "",
    "            fitj = Xmat @ coef",
    "            RSSj = np.sum((Yj - fitj) ** 2)",
    "",
    "            RSS[Nval - 1] = RSS[Nval - 1] + RSSj",
    "",
    "    # Now compute array of Mallow's C_p values.",
    "    Cpvals = np.zeros(Nmax, dtype=np.float64)",
    "    for i in range(1, Nmax + 1):",
    "        Cpvals[i - 1] = ((n - qq * Nmax) * RSS[i - 1] / RSS[Nmax - 1]) + 2 * qq * i - n",
    "",
    "    # order(Cpvec)[1L] in R: the (1-based) position of the smallest",
    "    # C_p value, using a stable sort so ties resolve to the first",
    "    # (smallest) index -- matching np.argmin's tie-breaking behavior.",
    "    return int(np.argmin(Cpvals)) + 1"
  ]
}
