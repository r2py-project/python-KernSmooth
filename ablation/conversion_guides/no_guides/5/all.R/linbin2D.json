{
  "imports": [
    "from typing import Any",
    "import numpy as np"
  ],
  "function_prototype": "def linbin2D(X: np.ndarray[Any, np.dtype[np.float64]], gpoints1: np.ndarray[Any, np.dtype[np.float64]], gpoints2: np.ndarray[Any, np.dtype[np.float64]]) -> np.ndarray[Any, np.dtype[np.float64]]:",
  "function_body": [
    "    # Creates the grid counts from a bivariate data set X",
    "    # over an equally-spaced set of grid points",
    "    # contained in \"gpoints1\"/\"gpoints2\" using the linear",
    "    # binning strategy.",
    "    n = X.shape[0]",
    "    x1 = X[:, 0]",
    "    x2 = X[:, 1]",
    "    M1 = len(gpoints1)",
    "    M2 = len(gpoints2)",
    "    a1 = gpoints1[0]",
    "    a2 = gpoints2[0]",
    "    b1 = gpoints1[M1 - 1]",
    "    b2 = gpoints2[M2 - 1]",
    "",
    "    # Reimplementation of the Fortran subroutine lbtwod (src/linbin2D.f)",
    "    # gcnts is built directly as an (M1, M2) array so that gcnts[row, col]",
    "    # matches R's matrix(out[[9L]], M1, M2), which reshapes the flat",
    "    # Fortran output in column-major order.",
    "    gcnts = np.zeros((M1, M2), dtype=np.float64)",
    "    delta1 = (b1 - a1) / (M1 - 1)",
    "    delta2 = (b2 - a2) / (M2 - 1)",
    "    for i in range(n):",
    "        lxi1 = ((x1[i] - a1) / delta1) + 1",
    "        lxi2 = ((x2[i] - a2) / delta2) + 1",
    "",
    "        # Find integer part of \"lxi1\" and \"lxi2\" (truncation toward zero,",
    "        # matching Fortran int())",
    "        li1 = int(lxi1)",
    "        li2 = int(lxi2)",
    "",
    "        rem1 = lxi1 - li1",
    "        rem2 = lxi2 - li2",
    "",
    "        if li1 >= 1 and li2 >= 1 and li1 < M1 and li2 < M2:",
    "            gcnts[li1 - 1, li2 - 1] += (1 - rem1) * (1 - rem2)",
    "            gcnts[li1, li2 - 1] += rem1 * (1 - rem2)",
    "            gcnts[li1 - 1, li2] += (1 - rem1) * rem2",
    "            gcnts[li1, li2] += rem1 * rem2",
    "",
    "    return gcnts"
  ]
}
